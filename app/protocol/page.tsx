import type { Metadata } from "next";
import { SiteChrome } from "../../components/SiteChrome";
export const metadata: Metadata = { title: "Evidence protocol" };
export default function ProtocolPage() { return <SiteChrome><main className="main prose" id="main-content"><p className="eyebrow">ProofLine protocol v2</p><h1>What the record can—and cannot—establish.</h1><p>ProofLine commits short encoded fragments before upload, independently re-hashes the exact received bytes, and returns a signed receipt only after durable storage. Each fragment commits to its predecessor, timing interval and telemetry root.</p><h2>First frame</h2><p>The first displayed frame is the first encoded frame in the app-signed capture chain. This is stronger than an uploaded file whose beginning can be removed without leaving a protocol trace. It is not a claim that Android exposes a universally trusted sensor-to-signature path.</p><h2>Two kinds of ending</h2><p><code>complete_with_signed_end</code> means the device signed a manifest covering each track’s count and final digest. <code>complete_as_server_received</code> means no valid ending arrived; the public player stops at the server’s highest contiguous receipt. The latter cannot prove the device never recorded an unreceived later frame.</p><h2>Device identity</h2><p>The Android app uses a non-exportable P-256 signing key in StrongBox or the hardware-backed TEE when available. The public device name is the SHA-256 fingerprint of that key—not an IMEI, serial number or MAC address. Browser captures use a non-exportable WebCrypto key and are visibly labeled <code>web_key</code>.</p><h2>Video, audio and sensors</h2><p>Original media fragments are immutable objects. Audio is bound to the same fragment chain and a shared capture group. Location, barometer, motion, gyroscope, camera settings and network observations are supporting context. Their signatures prove post-capture integrity, not that a compromised device reported truthfully.</p><h2>C2PA status</h2><p>ProofLine reports live and finalized C2PA validation separately. When the official SDK cannot author a conformant live binding, the site says <code>unsupported</code> and relies on the transparent ProofLine sidecar during capture. It never renames proprietary metadata as C2PA.</p><h2>Independent verification</h2><p>Evidence bundles include original bytes, canonical JSON, signatures, public keys, server receipts, validation output and a human-readable PDF. A verifier can re-run the checks without trusting the website UI.</p><div className="actions"><a className="secondary-button" href="/threat-model">Read the threat model</a><a className="primary-button" href="/capture">Record publicly</a></div></main></SiteChrome>; }
